{
  "name": "diagram-copilot",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --parallel -r dev"
  },
  "devDependencies": {
    "typescript": "~5.6",
    "vitest": "^2"
  }
}
