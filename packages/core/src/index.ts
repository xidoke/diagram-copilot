/**
 * @diagram-copilot/core
 *
 * Placeholder entrypoint for the core package. Real domain model,
 * diagram graph types, and shared utilities will land here.
 */
export const CORE_PACKAGE_NAME = "@diagram-copilot/core";

export function placeholder(): string {
  return CORE_PACKAGE_NAME;
}
