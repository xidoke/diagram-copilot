{
  "name": "@diagram-copilot/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "generate": "langium generate",
    "build": "pnpm run generate && tsc -p tsconfig.json"
  },
  "dependencies": {
    "langium": "^3.5.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "langium-cli": "^3.5.2",
    "typescript": "~5.6",
    "vitest": "^2"
  }
}
